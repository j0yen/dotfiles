[Service]
# PRD-grand-loop-listings-dedicated-clone requirement 6: same drop-in as
# grand-loop.service.d/listings-clone.conf -- grand-loop-demand.sh also
# invokes `mcphost-deploy publish|measure` and must resolve the same
# dedicated-clone default under systemd's near-empty environment.
Environment=MCPHOST_DEPLOY_LISTINGS_CLONE=%h/.cache/mcphost-deploy/prds-clone
